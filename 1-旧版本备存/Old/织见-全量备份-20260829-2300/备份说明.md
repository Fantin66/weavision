# 织见项目 全量备份说明（v4：全面修复优化后）

- **备份时间**：2026-08-29 23:00（GMT+8）
- **备份版本**：织见-全量备份-20260829-2300
- **备份原因**：本次全面修复与优化完成后存档（相对 v3 大改前备份：2210）

## 本次改动内容

### 一、三大核心问题
1. **画布-预览缩放联动锁定**：统一四渲染点（`syncPvContentZoom`/`syncMorphDom`/`renderPvContent`/`kickMorph`）为同一公式——iframe 固定逻辑视口（PC 1280 / 手机 375）+ `scale(可用世界宽/逻辑宽 × 画布zoom)`；pvZoom 用户缩放存 dataset 与画布 zoom 合并生效；滚轮/按钮/程序触发（zoomAt / fitAll / animateCamera / 状态栏重置）全路径均走 render→sync 链
2. **文件预览重构**：Markdown 完整 DOM 渲染（自研 mdToHtml/mdInline 渲染器：标题/粗斜/删除线/下划线++text++/行内代码/代码块/列表/表格/引用/任务清单/链接）+ 视频原生播放控制；新增全屏预览层（占屏 92% 留白边距、顶部工具栏、Esc/按钮退出、Alt+F 快捷键、全屏按钮已入 selbar+Dock）
3. **三风格独立优化**：fluent（亚克力通透.62、radius 18、柔阴影增强）；paper（radius 2 方角便利贴、笔触1.5、**gift 礼物包装缎带**）；minimal（无边框、fontScale 1.14 强字号分层、连线更细）

### 二、新增需求
- **Markdown 附件预览**：.md 文件在形变层走 DOM 富文本渲染（表格等完整展示）
- **工具栏完善**：下 Dock 编辑态 6→14 按钮（+粗体/斜体/删除线/下划线/行内代码/列表/链接/标题/分割线）；浮动栏 noteFormatBar 补 9 个 Markdown 插入按钮；修复 openTextEditor/closeEditor 未触发 renderDock 导致工具条状态不切换的根因
- **全屏功能**：见上
- **模板库→配色降级**：applyPalette 仅切换节点分层配色（不碰布局/风格/背景/字体），全部"模板库"文案清除
- **组织图线条优化**：ortho 从直角折线改为方向自适应的 S 形双弧下垂曲线

### 三、验证
- 配色降级/缩放联动(scale 0.334→0.167→0.334)/mdDoHtml 富文本/md 附件 DOM 表格/全屏显隐与 Esc/Dock 编辑态 14 按钮/5 风格渲染/回车Tab/7 布局/导图换向/便签不漂移/导出稳定/形变预览 全 OK，零报错

## 回滚
- 回滚到本版前：`Old/织见-全量备份-20260829-2210`
- 更早：`织见-全量备份-20260829-2140`（素材集成）、`-2128`（素材集成前）